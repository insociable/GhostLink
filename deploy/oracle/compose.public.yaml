name: ghostlink-public

services:
  ghostnode:
    build:
      context: ../..
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      GHOSTLINK_CONFIG: /config/ghostlink.toml
      GHOSTLINK_NODE_TOKEN_FILE: /run/secrets/ghostlink_node_token
      GHOSTLINK_RELAY_STATE_ID: ${GHOSTLINK_RELAY_STATE_ID:?Set a stable 128-bit relay state ID}
      GHOSTLINK_RELAY_STATE_KEY_FILE: /run/secrets/ghostlink_relay_state_key
    secrets:
      - ghostlink_node_token
      - ghostlink_relay_state_key
    volumes:
      - ghostnode_data:/data
      - ./ghostlink.toml:/config/ghostlink.toml:ro
    read_only: true
    tmpfs:
      - /tmp:size=16m,noexec,nosuid,nodev
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 10s
    networks:
      - backend

  caddy:
    image: caddy:2.11.4-alpine@sha256:de23def33b17fb5d1290b0f6c2add1d70780e52341896c00a4c8a2a2fe9d355e
    restart: unless-stopped
    depends_on:
      ghostnode:
        condition: service_healthy
    ports:
      - "443:443/tcp"
    environment:
      GHOSTLINK_PUBLIC_HOSTNAME: ${GHOSTLINK_PUBLIC_HOSTNAME:?Set the public GhostNode hostname}
      GHOSTLINK_ACME_EMAIL: ${GHOSTLINK_ACME_EMAIL:?Set the ACME account email}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    read_only: true
    tmpfs:
      - /tmp:size=16m,noexec,nosuid,nodev
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    security_opt:
      - no-new-privileges:true
    networks:
      - backend
      - edge

networks:
  backend:
    internal: true
  edge:

secrets:
  ghostlink_node_token:
    file: ${GHOSTLINK_NODE_TOKEN_FILE:?Set GHOSTLINK_NODE_TOKEN_FILE to the relay-token file path}
  ghostlink_relay_state_key:
    file: ${GHOSTLINK_RELAY_STATE_KEY_FILE:?Set GHOSTLINK_RELAY_STATE_KEY_FILE to the relay-state key file path}

volumes:
  ghostnode_data:
  caddy_data:
  caddy_config:
